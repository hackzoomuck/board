<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.mvc.board.mapper.PostMapper">
  <select id="findAll" resultType="Post">
    SELECT post_id, title, content
    FROM post
    WHERE delete_whether = FALSE
  </select>
  <select id="findByAll" parameterType="String" resultType="Post">
    SELECT post_id, title, content
    FROM post
    WHERE delete_whether = FALSE AND (title LIKE CONCAT('%',#{postAll},'%') OR content LIKE CONCAT('%',#{postAll},'%'))
  </select>
  <select id="findByTitle" parameterType="String" resultType="Post">
    SELECT post_id, title, content
    FROM post
    WHERE delete_whether = FALSE AND title LIKE CONCAT('%',#{title},'%')
  </select>
  <select id="findByContent" parameterType="String" resultType="Post">
    SELECT post_id, title, content
    FROM post
    WHERE delete_whether = FALSE AND content LIKE CONCAT('%',#{content},'%')
  </select>
  <select id="findByPostId" parameterType="int" resultType="Post">
    SELECT post_id, title, content
    FROM post
    WHERE post_id = #{postId}
  </select>
  <select id="search" parameterType="Search" resultType="Post">
    SELECT post_id, title, content FROM post WHERE delete_whether = FALSE
    <if test="search.postItem == 'postAll'">
    </if>
  </select>
</mapper>
